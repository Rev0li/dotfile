# session.conf — Layout 3 splits

# Split principal (en haut à gauche)
launch --cwd=~

# Split vertical à droite du premier (en haut à droite)
launch --location=vsplit --cwd=~

# Split horizontal sous le split de droite (en bas à droite)
launch --location=hsplit --cwd=~


